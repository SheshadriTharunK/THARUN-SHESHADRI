# Core FastAPI Stack
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy==2.0.23

# AI/ML Libraries (minimal, compatible versions)
pydantic-ai>=0.9.0
anthropic>=0.31.0
cohere>=5.0.0
passlib==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==4.1.1
cryptography==41.0.7
pyjwt==2.8.1

# Data Processing
transformers>=4.40.0
torch>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Optional - only if you really need these
# Remove extras that cause conflicts - use specific packages instead
# Instead of pydantic-ai-slim[all], use individual packages
